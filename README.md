# Section generator — frontend

React + Vite. Renders a JSON layout tree and lets you edit text in place.

```bash
npm install
npm run dev
```

App: `http://localhost:5173`  
API: `http://localhost:4000`

Try prompts: `pricing`, `hero`, `features`.

## Contributors

- Cursor
